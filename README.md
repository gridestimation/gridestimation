# GitHub Pages Static Website

Upload every file and folder in this package to the root of your GitHub repository.

Then open **Settings → Pages**, choose **Deploy from a branch**, select **main** and **/(root)**, and save.

This is a no-build static conversion. It loads React, Lucide icons, and Tailwind CSS from public CDNs, so no npm installation or build command is required.
